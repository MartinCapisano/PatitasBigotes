[pytest]
markers =
    concurrency: prueba de concurrencia que necesita un PostgreSQL real y ejercita locks a nivel de fila (with_for_update). Solo corre en el job backend-tests-postgres del CI; con SQLite se saltea.
